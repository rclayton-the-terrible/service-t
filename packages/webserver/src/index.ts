export { Webserver, WebPlugin, WebContext, ScopeDepsFn, Methods, CustomizeAppFn, DangerZone, InjectedHandler } from './model/Webserver';

import WebserverTemplate from './Webserver';

export default WebserverTemplate;
