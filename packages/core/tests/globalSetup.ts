// import makeServer from '../src/makeServer';
// import dotenv from 'dotenv';

export default async function globalSetup(): Promise<void> {
  // dotenv.config();
  // if (!process.env.USE_LOCAL_FOR_INTEG) {
  //   global.server = await makeServer({
  //     ...process.env,
  //     LOG_LEVEL: 'error',
  //   });
  //   await global.server.start();
  // }
}
